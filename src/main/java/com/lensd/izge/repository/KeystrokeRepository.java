package com.lensd.izge.repository;
import com.lensd.izge.entity.KeystrokeEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface KeystrokeRepository extends JpaRepository<KeystrokeEntity, Long> { 
    
}